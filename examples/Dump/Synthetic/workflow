Workflow the synthetic dump problem:

- edit file 'DumpGeometry.geo'

- generate configuration (--config) , electrode location (--loc) and schedule file (--schedule) and mesh (--mesh):

   python3 generate.py --geo DumpGeometry.geo --mesh --loc --wenner --config dump1

-